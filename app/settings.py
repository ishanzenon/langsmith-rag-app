"""Configuration utilities for loading environment variables and shared constants.

This module will centralize `.env` parsing, LangSmith/OpenAI keys, dataset names,
post catalogs, and any future settings so that other modules can rely on a single
source of truth.
"""
