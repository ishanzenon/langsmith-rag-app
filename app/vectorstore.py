"""Vector store construction and retrieval helpers.

Responsible for building the in-memory vector store from prepared documents and
exposing retriever interfaces (e.g., `get_retriever`) consumed by the RAG bot.
"""
